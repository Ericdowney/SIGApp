import { TodoItemPage } from './todoItem/todoItem.page';
import { HomePage } from './home/home';

export const Pages = {
    HomePage,
    TodoItemPage,
    all: [
        HomePage,
        TodoItemPage,
    ]
}